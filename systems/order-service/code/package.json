{
  "name": "order-service",
  "version": "0.1.0",
  "private": true,
  "description": "Order and checkout service for ImagiBricks.",
  "scripts": {
    "dev": "node src/index.js",
    "build": "echo 'Build order service'",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@imagibricks/contracts": "file:../contracts"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
