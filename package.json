{
  "name": "alpinebrick-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "contracts",
    "systems/catalog-service/code",
    "systems/inventory-service/code",
    "systems/order-service/code",
    "systems/affiliate-service/code",
    "systems/storefront/code",
    "systems/catalog-admin/code"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "deploy": "npm test && docker compose up --build -d",
    "lint": "echo 'Add repo-level linting here'",
    "test": "npm run test --workspaces --if-present"
  }
}
